overlay: 1.0.0
info:
  title: Overlay after-overlay-swagger.json => before-overlay-swagger.json
  version: 0.0.0
actions:
  - target: $..*["application/octet-stream"].schema
    update:
      "format": "binary"
